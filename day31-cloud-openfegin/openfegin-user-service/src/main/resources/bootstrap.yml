spring:
  profiles:
    active: dev
  application:
    name: user-service
  cloud:
    nacos:
      discovery:
        server-addr: 127.0.0.1:8848
      config:
        file-extension: yaml # 文件后缀
        server-addr: 127.0.0.1:8848

pattern:
  name: pattern本地配置